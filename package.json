{
  "name": "booking-platform",
  "version": "1.0.0",
  "scripts": {
    "start": "node index.js",
    "build:ui": "cd TravelEase && npm install && npm run build",
     "build": "npm run build:ui && npx cpy \"TravelEase/dist/**/*\" dist",
    "postinstall": "npm run build"
  },
  "dependencies": {
    "axios": "^1.10.0",
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "cpy-cli": "^5.0.0"
  }
}
